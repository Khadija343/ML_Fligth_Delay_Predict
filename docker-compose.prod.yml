services:

  frontend:
    build:
      context: ./frontend

    container_name: flight-delay-frontend-prod

    ports:
      - "5173:5173"

    environment:
      VITE_API_URL: https://ml-fligth-delay-predict.onrender.com

    restart: unless-stopped